name: push-dox

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
  
    runs-on: ubuntu-lates
    
    steps:
    - uses: mattnotmitt/doxygen-action@v1.9.4
      with: 'doc/Doxygen'
